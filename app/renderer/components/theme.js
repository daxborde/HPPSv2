import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    // type: 'dark',
    primary: {
      main: '#304ffe',
      // main: '#1d3d4c',
      // main: '#1dc0bd',
    },
    secondary: {
      main: '#304ffe',
    },
  },
});

export default theme;
